/**
 * File manager (explorer) — loads `ui-file-manager` / `ui-file-manager-content` and re-exports classes.
 */
export { default as FileManager, FileManagerContent } from "../ui/items/explorer/FileManager";
